#include "renderer/renderer.h"

namespace Vulkyrie::Renderer {
    // static Vulkyrie::Core::GraphicsAPI RendererAPI;
    //
    // Ref<Renderer> Renderer::Initialize(const Vulkyrie::Core::Platform &platform) {
    //     return CreateRef<Renderer>(platform);
    // }
}
