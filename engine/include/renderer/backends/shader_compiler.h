#pragma once

#include "vlkypch.h"

namespace Vulkyrie {

    class ShaderCompiler {
    public:
        ShaderCompiler();

        VE_DELETE_MOVE_AND_COPY(ShaderCompiler);

        ~ShaderCompiler();

    private:
    };
} // namespace Vulkyrie
