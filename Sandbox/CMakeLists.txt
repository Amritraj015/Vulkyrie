CMAKE_MINIMUM_REQUIRED(VERSION 3.22.2)
PROJECT(Sandbox VERSION 0.0.1 LANGUAGES CXX)

ADD_EXECUTABLE(${PROJECT_NAME} "Src/Entry.cpp")

TARGET_LINK_LIBRARIES(${PROJECT_NAME} VulkyrieEngine)
TARGET_INCLUDE_DIRECTORIES(${PROJECT_NAME} PRIVATE VulkyrieEngine)